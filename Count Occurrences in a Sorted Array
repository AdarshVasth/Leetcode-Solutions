class Solution {
public:
    int countOccurrences(vector<int>& arr, int target) {
        // Your code goes here
        int n = arr.size();
        int count =0;
        for(int i=0;i<n;i++){
            if(arr[i]==target){
                count+=1;
            }
        }
        return count;
    }
};
